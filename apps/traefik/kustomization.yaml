apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

helmCharts:
  - name: traefik
    version: 37.2.0
    repo: https://traefik.github.io/charts
    releaseName: traefik
    valuesFile: values.yaml
    valuesInline:
      installCRDs: true 
