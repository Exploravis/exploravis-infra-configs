apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-hq
  namespace: elasticsearch
spec:
  type: NodePort
  selector:
    app: elasticsearch-hq
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 32000
