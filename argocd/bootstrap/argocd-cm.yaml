apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
data:
  users.anonymous.enabled: "true"
  kustomize.buildOptions: --enable-helm
  
  policy.csv: |
    g, anonymous, role:admin
    
